/** @odoo-module **/

/**
 * Las validaciones de cantidad (decimales y negativos) se manejan
 * en order_summary_patch.js dentro de updateSelectedOrderline,
 * que es el punto central de entrada del numpad en Odoo 18.
 *
 * Este archivo se mantiene como placeholder documentado para
 * dejar clara la arquitectura del módulo.
 */
